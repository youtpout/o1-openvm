[package]
name = "mkinput"
version = "0.1.0"
edition = "2021"

[dependencies]
# std build: fixture parsing + host-side assembly of the guest's input.
pickles-verifier = { path = "../../../o1js-to-zkvm/crates/pickles-verifier" }
postcard = { version = "1", features = ["alloc"] }
hex = "0.4"
serde_json = "1"
